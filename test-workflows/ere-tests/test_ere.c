#include <stdio.h>

int main(int argc, char *argv[]) {
    // CLI options for testing:
    // --test-option
    // --another-option
    
    printf("Test ERE file with CLI options\n");
    return 0;
}
